/** Re-export shim: the scheduler moved to `../motion/scheduler` and is now shared with the scatter canvas layer. */

export type { Animation, Animation as GraphAnimation } from '../motion/scheduler';
export { AnimationScheduler } from '../motion/scheduler';
