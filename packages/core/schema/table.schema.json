{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/TableSpecSchema",
  "definitions": {
    "TableSpecSchema": {
      "$ref": "#/definitions/TableSpec",
      "description": "Root for the TableSpec subset schema (`table.schema.json`). The data-table visualization spec."
    },
    "TableSpec": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "const": "table",
          "description": "Discriminant: always \"table\"."
        },
        "data": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DataRow"
          },
          "description": "Data array: each element is a row."
        },
        "columns": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ColumnConfig"
          },
          "description": "Column definitions controlling display, sorting, formatting, and mini-charts."
        },
        "rowKey": {
          "type": "string",
          "description": "Optional field to use as a unique row identifier."
        },
        "chrome": {
          "$ref": "#/definitions/Chrome",
          "description": "Editorial chrome."
        },
        "theme": {
          "$ref": "#/definitions/ThemeConfig",
          "description": "Theme configuration overrides."
        },
        "darkMode": {
          "$ref": "#/definitions/DarkMode",
          "description": "Dark mode behavior."
        },
        "watermark": {
          "type": "boolean",
          "description": "Whether to show the tryOpenData.ai watermark. Defaults to true."
        },
        "search": {
          "type": "boolean",
          "description": "Enable client-side search/filter."
        },
        "pagination": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "object",
              "properties": {
                "pageSize": {
                  "type": "number"
                }
              },
              "required": [
                "pageSize"
              ],
              "additionalProperties": false
            }
          ],
          "description": "Pagination configuration. True for defaults, or an object with pageSize."
        },
        "stickyFirstColumn": {
          "type": "boolean",
          "description": "Whether to stick the first column during horizontal scroll."
        },
        "compact": {
          "type": "boolean",
          "description": "Compact mode: reduced padding and font sizes."
        },
        "responsive": {
          "type": "boolean",
          "description": "Whether the table adapts to container width. Defaults to true."
        },
        "animation": {
          "$ref": "#/definitions/AnimationSpec",
          "description": "Animation configuration for entrance animations."
        }
      },
      "required": [
        "type",
        "data",
        "columns"
      ],
      "additionalProperties": false,
      "description": "Table specification: input for data table visualizations.\n\nTables are a visualization type, not just an HTML grid. They support heatmap coloring, inline sparklines, sorted columns, search, and pagination."
    },
    "DataRow": {
      "type": "object",
      "additionalProperties": {},
      "description": "Data row: a plain object with string keys."
    },
    "ColumnConfig": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "Data field key (must match a key in the data rows)."
        },
        "label": {
          "type": "string",
          "description": "Display label for the column header. Defaults to the key if omitted."
        },
        "sortable": {
          "type": "boolean",
          "description": "Whether this column is sortable. Defaults to true."
        },
        "align": {
          "type": "string",
          "enum": [
            "left",
            "center",
            "right"
          ],
          "description": "Text alignment in the column. Defaults to \"left\" for text, \"right\" for numbers."
        },
        "width": {
          "type": "string",
          "description": "Explicit column width (CSS value like \"200px\" or \"20%\"). Auto-sized if omitted."
        },
        "format": {
          "type": "string",
          "description": "Number/date format string (d3-format or d3-time-format), or semantic keyword `'percent'` / `'currency'`. e.g. \",.0f\", \"%Y-%m-%d\", \"percent\". Tables always show full precision (no compact notation)."
        },
        "heatmap": {
          "$ref": "#/definitions/HeatmapColumnConfig",
          "description": "Heatmap: color the cell background based on numeric value."
        },
        "bar": {
          "$ref": "#/definitions/BarColumnConfig",
          "description": "Inline bar: render a proportional bar in the cell."
        },
        "sparkline": {
          "$ref": "#/definitions/SparklineColumnConfig",
          "description": "Sparkline: render a mini line/bar chart in the cell."
        },
        "image": {
          "$ref": "#/definitions/ImageColumnConfig",
          "description": "Image: render the cell value as an image URL."
        },
        "flag": {
          "type": "boolean",
          "description": "Flag: render the cell value as a country flag emoji or image."
        },
        "categoryColors": {
          "$ref": "#/definitions/CategoryColorsConfig",
          "description": "Category colors: color-code the cell based on its categorical value."
        },
        "autoAssign": {
          "type": "boolean",
          "description": "When true, auto-assign palette colors to values not in categoryColors. Default: false."
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false,
      "description": "Configuration for a single table column.\n\nAt minimum, `key` identifies the data field. All other properties control display, sorting, formatting, and visual features.\n\nOnly one visual feature (heatmap, bar, sparkline, image, flag, categoryColors) should be active per column. If multiple are provided, the engine picks the first one in this precedence order: sparkline > bar > heatmap > image > flag > categoryColors."
    },
    "HeatmapColumnConfig": {
      "type": "object",
      "properties": {
        "palette": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "Color palette name or array of color stops. Uses sequential palette from theme if a string name is provided."
        },
        "domain": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "minItems": 2,
          "maxItems": 2,
          "description": "Explicit domain [min, max] for the color scale. Auto-derived from data if omitted."
        },
        "colorByField": {
          "type": "string",
          "description": "Use a different field's values for coloring while displaying this column's values. Useful for coloring a label column based on a numeric column."
        }
      },
      "additionalProperties": false,
      "description": "Heatmap coloring configuration for a table column."
    },
    "BarColumnConfig": {
      "type": "object",
      "properties": {
        "maxValue": {
          "type": "number",
          "description": "Maximum value for the bar scale. Auto-derived from data if omitted."
        },
        "color": {
          "type": "string",
          "description": "Bar fill color. Uses the first categorical palette color if omitted."
        }
      },
      "additionalProperties": false,
      "description": "Inline bar configuration for a table column."
    },
    "SparklineColumnConfig": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "line",
            "bar",
            "column"
          ],
          "description": "Sparkline chart type. Defaults to \"line\"."
        },
        "valuesField": {
          "type": "string",
          "description": "Field containing the array of values to plot."
        },
        "color": {
          "type": "string",
          "description": "Sparkline color. Uses the first categorical palette color if omitted."
        }
      },
      "additionalProperties": false,
      "description": "Inline sparkline configuration for a table column."
    },
    "ImageColumnConfig": {
      "type": "object",
      "properties": {
        "width": {
          "type": "number",
          "description": "Image width in pixels. Defaults to 24."
        },
        "height": {
          "type": "number",
          "description": "Image height in pixels. Defaults to 24."
        },
        "rounded": {
          "type": "boolean",
          "description": "Whether to apply border-radius for rounded/circular images."
        }
      },
      "additionalProperties": false,
      "description": "Image cell configuration for a table column."
    },
    "CategoryColorsConfig": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Category color mapping for a table column. Maps category values to CSS colors."
    },
    "Chrome": {
      "type": "object",
      "properties": {
        "eyebrow": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/ChromeText"
            }
          ],
          "description": "Editorial kicker/category label rendered above the title. Typically uppercase, tracked, and tinted with the accent color (e.g. \"Equities · Single Ticker\"). Term follows IBM Carbon and Atlassian Design System conventions; not part of Vega-Lite's title model."
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/ChromeText"
            }
          ],
          "description": "Main title displayed above the visualization."
        },
        "subtitle": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/ChromeText"
            }
          ],
          "description": "Subtitle displayed below the title, typically providing context."
        },
        "source": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/ChromeText"
            }
          ],
          "description": "Data source attribution, displayed below the chart area."
        },
        "byline": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/ChromeText"
            }
          ],
          "description": "Author or organization byline."
        },
        "footer": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/ChromeText"
            }
          ],
          "description": "Footer text, displayed at the very bottom."
        },
        "brand": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/ChromeText"
            }
          ],
          "description": "Right-anchored brand block on the footer row, paired with a small accent dot to its left. Visually balances the source/byline left-anchored text. When set, suppresses the default `tryOpenData.ai` watermark for this chart."
        }
      },
      "additionalProperties": false,
      "description": "Editorial chrome elements: eyebrow, title, subtitle, source attribution, byline, footer. These are first-class structural elements, not string-only afterthoughts. Each element can be a simple string or a ChromeText object with style overrides."
    },
    "ChromeText": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The text content to display."
        },
        "style": {
          "$ref": "#/definitions/ChromeTextStyle",
          "description": "Optional style overrides. Theme defaults are used for any omitted property."
        },
        "offset": {
          "$ref": "#/definitions/AnnotationOffset",
          "description": "Pixel offset for fine-tuning position."
        }
      },
      "required": [
        "text"
      ],
      "additionalProperties": false,
      "description": "A chrome text element with optional style overrides."
    },
    "ChromeTextStyle": {
      "type": "object",
      "properties": {
        "fontSize": {
          "type": "number",
          "description": "Font size in pixels."
        },
        "fontWeight": {
          "type": "number",
          "description": "Font weight (400 = normal, 600 = semibold, 700 = bold)."
        },
        "fontFamily": {
          "type": "string",
          "description": "Font family override."
        },
        "color": {
          "type": "string",
          "description": "Text color (CSS color string)."
        }
      },
      "additionalProperties": false,
      "description": "Style overrides for a chrome text element."
    },
    "AnnotationOffset": {
      "type": "object",
      "properties": {
        "dx": {
          "type": "number",
          "description": "Horizontal pixel offset."
        },
        "dy": {
          "type": "number",
          "description": "Vertical pixel offset."
        }
      },
      "additionalProperties": false,
      "description": "Pixel offset for fine-grained annotation positioning."
    },
    "ThemeConfig": {
      "type": "object",
      "properties": {
        "colors": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "object",
              "properties": {
                "categorical": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Categorical palette for nominal data (array of CSS color strings)."
                },
                "sequential": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  },
                  "description": "Sequential palettes keyed by name. Each is an array of color stops."
                },
                "diverging": {
                  "type": "object",
                  "additionalProperties": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  },
                  "description": "Diverging palettes keyed by name. Each is an array of color stops with a neutral midpoint."
                },
                "background": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Background color. Accepts a TokenValue for light/dark pairs."
                },
                "text": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Default text color. Accepts a TokenValue for light/dark pairs."
                },
                "gridline": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Gridline color. Accepts a TokenValue for light/dark pairs."
                },
                "axis": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Axis line and tick color. Accepts a TokenValue for light/dark pairs."
                },
                "annotationFill": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Annotation fill color. Accepts a TokenValue for light/dark pairs."
                },
                "annotationText": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Annotation text color. Accepts a TokenValue for light/dark pairs."
                },
                "positive": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Semantic color for positive/up-trend values. Accepts a TokenValue for light/dark pairs."
                },
                "negative": {
                  "$ref": "#/definitions/TokenValue",
                  "description": "Semantic color for negative/down-trend values. Accepts a TokenValue for light/dark pairs."
                }
              },
              "additionalProperties": false
            }
          ],
          "description": "Color palette overrides. Pass a flat string[] as shorthand for categorical colors, or an object for full control over categorical, sequential, diverging, etc."
        },
        "fonts": {
          "type": "object",
          "properties": {
            "family": {
              "type": "string",
              "description": "Primary font family."
            },
            "mono": {
              "type": "string",
              "description": "Monospace font family (for tabular numbers)."
            },
            "sizes": {
              "type": "object",
              "properties": {
                "title": {
                  "type": "number",
                  "description": "Chart title. Default: 26."
                },
                "subtitle": {
                  "type": "number",
                  "description": "Subtitle below the title. Default: 14."
                },
                "body": {
                  "type": "number",
                  "description": "Body text (tooltips, legend labels). Default: 13."
                },
                "small": {
                  "type": "number",
                  "description": "Small text (source line, footer). Default: 11."
                },
                "axisTick": {
                  "type": "number",
                  "description": "Axis tick labels. Default: 11."
                },
                "metricLabel": {
                  "type": "number",
                  "description": "KPI metric uppercase label. Default: 10."
                },
                "metricValue": {
                  "type": "number",
                  "description": "KPI metric primary value. Default: 22. Delta/secondary derive from this."
                }
              },
              "additionalProperties": false,
              "description": "Font size overrides in pixels. Partial — only specified keys are overridden."
            },
            "weights": {
              "type": "object",
              "properties": {
                "normal": {
                  "type": "number",
                  "description": "Normal text weight. Default: 450."
                },
                "medium": {
                  "type": "number",
                  "description": "Medium text weight. Default: 550."
                },
                "semibold": {
                  "type": "number",
                  "description": "Semibold text weight. Default: 590."
                },
                "bold": {
                  "type": "number",
                  "description": "Bold text weight. Default: 700."
                }
              },
              "additionalProperties": false,
              "description": "Font weight overrides. Partial — only specified keys are overridden."
            }
          },
          "additionalProperties": false,
          "description": "Font overrides."
        },
        "spacing": {
          "type": "object",
          "properties": {
            "padding": {
              "type": "number",
              "description": "Padding inside the chart container."
            },
            "chromeGap": {
              "type": "number",
              "description": "Gap between chrome elements (title to subtitle, etc.)."
            },
            "chromeToChart": {
              "type": "number",
              "description": "Gap between the last chrome element and the chart area."
            },
            "chartToFooter": {
              "type": "number",
              "description": "Gap between chart area and source/footer below."
            },
            "axisMargin": {
              "type": "number",
              "description": "Internal padding within the chart area (axes margins)."
            },
            "xAxisHeight": {
              "type": "number",
              "description": "Height reserved below chart area for x-axis tick labels. Increase when large axisTick font sizes cause label clipping."
            },
            "xAxisLabelPadding": {
              "type": "number",
              "description": "Gap in pixels between the x-axis line and tick label text. Increase when larger axisTick fonts sit too close to the axis line."
            }
          },
          "additionalProperties": false,
          "description": "Spacing overrides in pixels."
        },
        "borderRadius": {
          "type": "number",
          "description": "Border radius for chart container and tooltips."
        },
        "chrome": {
          "type": "object",
          "properties": {
            "eyebrow": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChromeThemeOverride"
                }
              ],
              "description": "Eyebrow (kicker) style override."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChromeThemeOverride"
                }
              ],
              "description": "Title style override."
            },
            "subtitle": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChromeThemeOverride"
                }
              ],
              "description": "Subtitle style override."
            },
            "source": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChromeThemeOverride"
                }
              ],
              "description": "Source/attribution style override."
            },
            "byline": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChromeThemeOverride"
                }
              ],
              "description": "Byline style override."
            },
            "footer": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/definitions/ChromeThemeOverride"
                }
              ],
              "description": "Footer style override."
            }
          },
          "additionalProperties": false,
          "description": "Per-element chrome style overrides. Pass a plain string for color-only (backward-compatible), or a ChromeThemeOverride object for full typography control (color, fontSize, fontWeight, lineHeight)."
        },
        "seriesStrategy": {
          "$ref": "#/definitions/SeriesStrategy",
          "description": "Series color assignment strategy. Default: 'palette' (full categorical palette always, zero visual drift)."
        }
      },
      "additionalProperties": false,
      "description": "User-facing theme configuration for overriding defaults. All fields are optional. The engine deep-merges these onto the default theme."
    },
    "TokenValue": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "properties": {
            "light": {
              "type": "string"
            },
            "dark": {
              "type": "string"
            }
          },
          "required": [
            "light",
            "dark"
          ],
          "additionalProperties": false
        }
      ],
      "description": "A color token value: either a plain CSS color string, or a light/dark pair. When a pair is given, the resolver picks the appropriate value based on dark mode state. When a plain string is given, dark-mode adaptation derives the dark value algorithmically (existing behavior)."
    },
    "ChromeThemeOverride": {
      "type": "object",
      "properties": {
        "color": {
          "$ref": "#/definitions/TokenValue",
          "description": "Text color. Accepts a TokenValue for light/dark pairs."
        },
        "fontSize": {
          "type": "number",
          "description": "Font size in pixels."
        },
        "fontWeight": {
          "type": "number",
          "description": "Font weight."
        },
        "lineHeight": {
          "type": "number",
          "description": "Line height multiplier."
        }
      },
      "additionalProperties": false,
      "description": "Per-element chrome theme override. Pass a plain string (color only, backward-compatible) or an object with full typography control."
    },
    "SeriesStrategy": {
      "type": "string",
      "enum": [
        "palette",
        "accent-neutral"
      ],
      "description": "Series color assignment strategy.\n\n- `'palette'` — always assign from the full categorical palette (current   default, zero visual drift).\n- `'accent-neutral'` — assign by series count: a single series gets the   accent (first categorical color) only; 2-4 series get accent for the   first and neutral grays for the rest; 5+ fall back to the full palette."
    },
    "DarkMode": {
      "type": "string",
      "enum": [
        "auto",
        "force",
        "off"
      ],
      "description": "Dark mode behavior.\n\n- `\"auto\"` - Checks the `prefers-color-scheme` media query to detect the   user's system-level preference. This does NOT detect class-based dark mode   toggles (e.g. `document.documentElement.classList.toggle('dark')`). If your   app uses class-based dark mode, use VizThemeProvider with `\"force\"` or   `\"off\"` instead of `\"auto\"` and toggle based on your app's state.\n- `\"force\"` - Always render in dark mode regardless of system preference.\n- `\"off\"` - Always render in light mode (default).\n\nAll components (Chart, Sankey, Graph) inherit darkMode from VizThemeProvider when no explicit prop is passed."
    },
    "AnimationSpec": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "#/definitions/AnimationConfig"
        }
      ],
      "description": "Animation spec property.\n- true: enable entrance animation with sensible defaults\n- false/omitted: no animation (current behavior)\n- AnimationConfig: full control via enter/update/exit phases"
    },
    "AnimationConfig": {
      "type": "object",
      "properties": {
        "enter": {
          "anyOf": [
            {
              "$ref": "#/definitions/AnimationPhaseConfig"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Entrance animation when chart first renders."
        },
        "update": {
          "anyOf": [
            {
              "$ref": "#/definitions/AnimationPhaseConfig"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Transition animation when data updates."
        },
        "exit": {
          "anyOf": [
            {
              "$ref": "#/definitions/AnimationPhaseConfig"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Exit animation when marks are removed."
        },
        "annotationDelay": {
          "type": "number",
          "description": "Delay before annotations animate in (ms after marks). Default: 200."
        }
      },
      "additionalProperties": false,
      "description": "Full animation config object. Structured as enter/update/exit phases following Vega's encoding set model."
    },
    "AnimationPhaseConfig": {
      "type": "object",
      "properties": {
        "duration": {
          "type": "number",
          "description": "Duration in ms. Default: 500 for enter."
        },
        "ease": {
          "$ref": "#/definitions/AnimationEase",
          "description": "Easing preset. Default: 'smooth'."
        },
        "stagger": {
          "anyOf": [
            {
              "$ref": "#/definitions/AnimationStagger"
            },
            {
              "type": "boolean"
            }
          ],
          "description": "Stagger config. true = defaults, false = no stagger. Default: true for enter."
        }
      },
      "additionalProperties": false,
      "description": "Animation phase config. Follows Vega's enter/update/exit model. Each phase can be true (use defaults) or a config object."
    },
    "AnimationEase": {
      "type": "string",
      "enum": [
        "smooth",
        "snappy"
      ],
      "description": "Named easing presets for entrance animations. Uses CSS linear() curves. Named 'ease' (not 'easing') to match Vega convention."
    },
    "AnimationStagger": {
      "type": "object",
      "properties": {
        "delay": {
          "type": "number",
          "description": "Delay between each element in ms. Default: 80"
        },
        "order": {
          "type": "string",
          "enum": [
            "index",
            "value",
            "reverse"
          ],
          "description": "Ordering strategy. Default: 'index' (DOM order)"
        }
      },
      "additionalProperties": false,
      "description": "Stagger configuration for sequential element reveal."
    }
  }
}
