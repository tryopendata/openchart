node_modules/
dist/
build/
.turbo/
.svelte-kit/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.local
*.log
.vscode/
.idea/
.playwright-cli
./claude/worktrees
.npmrc
*.png

# Playwright visual regression
test-results/
playwright-report/
e2e/visual/__screenshots__/**/*-actual.png
e2e/visual/__screenshots__/**/*-diff.png
# Keep committed baselines under e2e/visual/__screenshots__/ despite the
# root-level *.png exclusion above.
!e2e/visual/__screenshots__/**/*.png
# Keep the social-preview asset: it ships to the built site and is referenced
# by the OG/twitter:image meta tags, so it must be committed.
!examples/public/og-preview.png

*.tgz

# Static render example output
examples/output.svg

# Scratch Ladle build output (e.g. `ladle build --outDir tmp/...` during verification)
examples/tmp/
